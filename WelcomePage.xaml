<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="odev.WelcomePage"
             BackgroundColor="{DynamicResource BackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Padding="30,0"  Spacing="25">
            <Label
            
            />
            <Image
                Source="me.png" HeightRequest="330" Aspect="AspectFit" />
            <Label 
                Text="Ahmad Abid Yamin" FontAttributes="Bold" Style="{StaticResource Headline}" SemanticProperties.HeadingLevel="Level1" HorizontalOptions="Center" />

            <Label
                Text="Bartın Üniversitesi Bilgisayar Müh." Style="{StaticResource SubHeadline}" SemanticProperties.HeadingLevel="Level2" HorizontalOptions="Center" />
           
            <Label
                Text="ÖDEV - 3" Style="{StaticResource SubHeadline}" SemanticProperties.HeadingLevel="Level3" HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
